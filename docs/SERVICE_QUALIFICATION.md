# Service Qualification

Bounded repository-side tests for LeadLeak Fix, Workflow Clinic, Site Rescue Desk, and Fintech Signal Desk. No DNS, billing, credentials, or production-data mutations are performed by the workflow.

The workflow uses no dependency cache because this repository does not currently track an npm lockfile.


Trigger revision: qualification workflow cache compatibility fix is included on main.
