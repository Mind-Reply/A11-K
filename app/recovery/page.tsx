const phases=[['Detect','Build failures, broken links, missing assets, policy drift and deployment anomalies become evidence.'],['Contain','Jobs fail closed and do not mutate billing, credentials, DNS, ownership or customer records to make a green dashboard.'],['Restore','Use the last verified artifact or rollback path, then rerun focused checks.'],['Resume','Resume only after the relevant gate is green and the evidence record explains what changed and what remains blocked.']];
export default function Recovery(){return <main className="min-h-screen bg-[#070807] px-5 py-10 text-white sm:px-10 lg:px-16"><div className="mx-auto max-w-5xl"><a href="/flight-deck">← Flight Deck</a><p className="mt-16 text-[10px] uppercase tracking-[.3em] text-amber-200/70">05 / RECOVERY</p><h1 className="mt-5 text-5xl font-light tracking-[-.055em] sm:text-7xl">Fail safely. Resume deliberately.</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-white/50">Recovery is itself an operating capability: detect, contain, restore, verify and resume without hiding failure.</p><section className="mt-12 grid gap-4 md:grid-cols-2">{phases.map(p=><article className="rounded-2xl border border-white/10 p-6" key={p[0]}><h2>{p[0]}</h2><p className="mt-3 text-white/45">{p[1]}</p></article>)}</section></div></main>}