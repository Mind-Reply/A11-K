import RuntimeValidation from '../components/RuntimeValidation';

const checks=[['GitHub source of truth','PASS','Repository commits and files are the canonical implementation record.'],['Production route checks','PASS','Executable estate health checks HTTP surfaces from the deployed runtime.'],['Revenue guard','PASS','Revenue runtime remains deterministic and model-optional.'],['External payment reconciliation','BLOCKED','Server-side Stripe variables are not configured; hosted checkout is separate.'],['Owner-gated actions','PASS','Money movement, DNS, credentials, destructive changes and outreach remain gated.']];
export default function Evidence(){return <main className="min-h-screen bg-[#070807] px-5 py-10 text-white sm:px-10 lg:px-16"><div className="mx-auto max-w-5xl"><a href="/flight-deck">← Flight Deck</a><p className="mt-16 text-[10px] uppercase tracking-[.3em] text-white/35">04 / EVIDENCE ROOM</p><h1 className="mt-5 text-5xl font-light tracking-[-.055em] sm:text-7xl">Proof before posture.</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-white/50">Verified state, automated checks, external verification and owner decisions are kept separate.</p><div className="mt-12 space-y-3">{checks.map(c=><article className="rounded-2xl border border-white/10 bg-white/[.02] p-6" key={c[0]}><div className="flex flex-wrap justify-between gap-4"><b>{c[0]}</b><span className={c[1]==='BLOCKED'?'text-amber-200':'text-green-200'}>{c[1]}</span></div><p className="mt-3 text-sm leading-6 text-white/45">{c[2]}</p></article>)}</div><RuntimeValidation /><p className="mt-8 text-xs text-white/35">No secrets, payment details, customer PII or private ledger data are published as evidence.</p></div></main>}
